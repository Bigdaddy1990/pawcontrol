name: hassfest

on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: "0 3 * * *"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run hassfest
        uses: home-assistant/actions/hassfest@master
